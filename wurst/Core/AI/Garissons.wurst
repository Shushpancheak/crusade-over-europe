package Garissons

import Main
import UnitTools

init
  EventListener.add(EVENT_PLAYER_UNIT_ATTACKED) ->
    let u = GetTriggerUnit()
    if (u.isCity() or uCastles.has(u.getTypeId())) and not u.getOwner().isIngame()
      IssueImmediateOrder(u, oGarrison)
